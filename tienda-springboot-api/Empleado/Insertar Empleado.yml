info:
  name: Insertar Empleado
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/empleados
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

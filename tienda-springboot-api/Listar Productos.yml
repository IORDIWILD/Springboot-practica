info:
  name: Listar Productos
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/productos/precio
  params:
    - name: min
      value: "100"
      type: query
      disabled: true
    - name: max
      value: "1000"
      type: query
      disabled: true
  body:
    type: text
    data: Mesa de centro
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

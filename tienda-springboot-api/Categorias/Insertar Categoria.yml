info:
  name: Insertar Categoria
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/categorias
  body:
    type: json
    data: |-
      {
        "categoriaPadreId" : 7,
        "nombre" : "Utencilios"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Actualizar Cliente
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/clientes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
